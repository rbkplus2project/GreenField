export const showMenu = (z) => {
    return {
        type: 'SHOW_MENU',
        showMenu: z
    }
}

export const showsetting = (z) => {
    return {
        type: 'SHOW_setting',
        showsetting: z
    }
}
